# Error handling

Demonstrates fallible functions and error handling.

## Run

```bash
quadc -r errors.qd
```

## Features

- Fallible functions (`fn name()!`)
- Error checking with `if`/`else`
- Error propagation with `!`
