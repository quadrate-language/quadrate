struct Point {
	x: f64
	y: f64
}

fn (self:Point) magnitude( -- result:f64) {
	self.x self.x * self.y self.y * + sqrt
}

fn (self:Point) translate(dx:f64 dy:f64 -- result:Point) {
	Point {
		x = self.x dx +
		y = self.y dy +
	}
}

pub fn (self:Point) to_string( -- result:str) {
	"point"
}
