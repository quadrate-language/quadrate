fn may_fail(x:i64 -- result:i64)! {
	x 0 < if { "negative" 1 panic }
	x
}

fn caller() {
	5 may_fail switch {
		Ok { -> val val print nl }
		_ { err drop drop "failed" print nl }
	}
}

fn propagate(x:i64 -- result:i64)! {
	x may_fail switch {
		Ok {
		}
		_ { err panic }
	}
}
