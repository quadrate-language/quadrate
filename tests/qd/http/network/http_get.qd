// Test HTTP GET request
use http
use strings

fn main( -- ) {
	"https://example.com" http::get switch {
		Ok {
			-> resp
			resp @status 200 eq
			if {
				"status ok" print nl
			}
			"body length:" print resp @body strings::len print nl
			resp http::close
		}
		_ {
			-> err
			"error:" print err print nl
		}
	}
}
