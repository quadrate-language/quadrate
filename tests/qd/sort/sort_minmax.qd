use mem
use sort

fn main() {
	// Create array [3, 1, 4, 1, 5]
	5 8 * mem::alloc -> arr
	arr 0 3 mem::set
	arr 8 1 mem::set
	arr 16 4 mem::set
	arr 24 1 mem::set
	arr 32 5 mem::set

	arr 5 sort::min print nl
	arr 5 sort::max print nl

	arr mem::free
}
