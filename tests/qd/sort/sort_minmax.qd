use mem
use sort

fn main() {
	// Create array [3, 1, 4, 1, 5]
	5 8 * mem::alloc -> arr
	3 arr 0 mem::set
	1 arr 8 mem::set
	4 arr 16 mem::set
	1 arr 24 mem::set
	5 arr 32 mem::set

	arr 5 sort::min print nl
	arr 5 sort::max print nl

	arr mem::free
}
