// Test: >>field storing a []i64 in a raw ptr field should fail. Both sides are
// pointers, so only the field's own type separates them.
struct C {
	data:ptr
	n:i64
}

fn main() {
	C { data = 0 n = 1 } -> c
	[3 1 2] -> a
	c a >>data drop
}
