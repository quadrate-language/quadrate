// A literal zero *integer* divisor is knowable at compile time. Reaching it at
// run time is a fatal error that kills the process, so report it here instead.
// Floats are not included: x 0.0 / is defined by IEEE 754, not an error.
fn main() {
	1 0 / print nl
	1 0 % print nl
}
