// Test: ? used in a non-fallible function should error
fn might_fail(x:i64 -- result:i64)! {
	-> x
	x 0 < if { "negative" 1 panic }
	x 10 +
}

fn not_fallible(x:i64 -- result:i64) {
	-> x
	x might_fail?
}

fn main() {
	5 not_fallible print nl
}
