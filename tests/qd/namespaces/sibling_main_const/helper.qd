// Helper file that references constants from main.qd
use fmt
use str

fn compute(x:i64 -- result:i64) {
	x MULTIPLIER *
}

fn format_value(x:i64 -- result:str) {
	-> x
	x "%d" fmt::sprintf -> num_str
	PREFIX num_str str::concat
}
