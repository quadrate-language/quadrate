// Sibling defining Circle with Point (from main) as nested field
use math

struct Circle {
	center:Point
	radius:f64
}

fn make_circle(p:Point r:f64 -- c:Circle) {
	-> r -> p
	Circle { center = p, radius = r }
}

fn circle_area(c:Circle -- area:f64) {
	-> c
	math::Pi c @radius c @radius * *
}
