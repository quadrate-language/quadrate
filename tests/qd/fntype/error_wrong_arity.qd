// Test: passing fn(i64 i64 -- i64) where fn(i64 -- i64) expected should fail
fn apply(x:i64 f:fn(i64 -- i64) -- result:i64) {
	x f call
}

fn main() {
	5 fn (a:i64 b:i64 -- r:i64) { a b + } apply print nl
}
