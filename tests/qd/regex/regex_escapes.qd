use regex

fn main() {
	// Escaped special characters
	"a\\.b" "a.b" regex::is_match print nl
	"a\\.b" "axb" regex::is_match print nl

	// Escaped backslash
	"a\\\\b" "a\\b" regex::is_match print nl

	// Newline escape
	"a\\nb" "a\nb" regex::is_match print nl

	// Tab escape
	"a\\tb" "a\tb" regex::is_match print nl

	// Literal special chars via escape
	"\\+" "+" regex::is_match print nl
	"\\*" "*" regex::is_match print nl
	"\\?" "?" regex::is_match print nl
	"\\|" "|" regex::is_match print nl
}
