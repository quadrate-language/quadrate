use regex

fn main() {
	// Note: Full-match semantics means anchors (^ and $) are implicit
	// Patterns match the entire string by default
	// These tests verify full-match behavior

	"hello" "hello" regex::is_match print nl
	"hello" "hello world" regex::is_match print nl
	"hello" "say hello" regex::is_match print nl

	// Prefix match (would need partial match mode)
	"hello.*" "hello world" regex::is_match print nl

	// Suffix match (would need partial match mode)
	".*world" "hello world" regex::is_match print nl

	// Full pattern
	"hello world" "hello world" regex::is_match print nl
}
