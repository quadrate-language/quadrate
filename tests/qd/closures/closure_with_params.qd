// Test: Closure with both input parameters and captures
fn main() {
    100 -> base
    fn (x:i64 -- r:i64) { x base add } -> add_to_base
    5 add_to_base call print nl
    25 add_to_base call print nl
}
