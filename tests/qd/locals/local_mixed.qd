// Test mixing stack parameters and computed locals
fn process( base:i64 multiplier:i64 -- result:i64 ) {
    -> mult
    -> val
    // Compute derived value
    val mult mul -> product
    // Use both original and derived
    val product add
}

fn main( -- ) {
    10 3 process . nl
}
