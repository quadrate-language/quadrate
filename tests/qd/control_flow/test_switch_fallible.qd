// Test switch with fallible functions (alternative to if)
use io

fn main() {
    // Test switch with bool from fallible function
    "/tmp/test_switch_fallible.txt" io::Write io::open switch {
        Ok {
            -> file
            "opened" print nl
            file io::close
        }
        _ {
            "failed" print nl
        }
    }
}
