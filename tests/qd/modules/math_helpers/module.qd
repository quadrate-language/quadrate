// Math helper functions
pub fn abs(f64 -- f64) {
	dup 0.0 lt
	if {
		neg
	}
}

pub fn max(f64 f64 -- f64) {
	dup rot dup rot
	gt
	if {
		drop
	} else {
		nip
	}
}

pub fn square(f64 -- f64) {
	dup mul
}

pub fn add_ten(f64 -- f64) {
	10.0 add
}
