use math

fn main( -- ) {
	// Test constants
	math::Pi . nl
	math::E . nl
	math::Tau . nl

	// Test basic trig
	0.0 math::sin . nl
	0.0 math::cos . nl

	// Test sqrt and sq
	16.0 math::sqrt . nl
	5.0 math::sq . nl

	// Test abs
	-42.0 math::abs . nl

	// Test min/max
	5.0 10.0 math::min . nl
	5.0 10.0 math::max . nl

	// Test rounding
	3.7 math::ceil . nl
	3.7 math::floor . nl
	3.5 math::round . nl

	// Test deg_to_rad (module function)
	180.0 math::deg_to_rad math::Pi sub math::abs 0.001 lt . nl

	// Test clamp (module function)
	15.0 0.0 10.0 math::clamp . nl
	-5.0 0.0 10.0 math::clamp . nl
	5.0 0.0 10.0 math::clamp . nl

	// Test lerp (module function)
	0.0 10.0 0.5 math::lerp . nl
}
