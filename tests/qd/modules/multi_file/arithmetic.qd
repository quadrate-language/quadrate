// Arithmetic operations
pub stack fn increment(f64 -- f64) {
	1.0 add
}

pub stack fn decrement(f64 -- f64) {
	1.0 sub
}

pub stack fn double(f64 -- f64) {
	2.0 mul
}

pub stack fn half(f64 -- f64) {
	2.0 div
}
