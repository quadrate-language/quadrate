// Test defer blocks in modules
use defer_ops

fn main( -- ) {
	// Test simple defer
	5.0 defer_ops::with_cleanup
	print nl
	// Test nested defer (LIFO order)
	10.0 defer_ops::nested_defer
	print nl
	// Test defer with computation
	30.0 20.0 defer_ops::defer_with_computation
	print nl
	// Test defer with early return (positive)
	5.0 defer_ops::early_return
	print nl
	// Test defer with early return (negative)
	-3.0 defer_ops::early_return
	print nl
}
