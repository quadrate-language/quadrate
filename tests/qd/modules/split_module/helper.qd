// Helper functions for split_module
// These functions will be part of the split_module namespace
pub stack fn double(f64 -- f64) {
	dup add
}

pub stack fn negate(f64 -- f64) {
	neg
}

pub fn get_hundred( -- v:f64) {
	100.0
}
