use hof

fn main() {
    // bi applies two functions to same value
    5 fn (x:i64 -- r:i64) { 2 * } fn (x:i64 -- r:i64) { 3 + } hof::bi
    print " " print print nl

    // Check both positive and even
    6 fn (x:i64 -- r:i64) { 0 > } fn (x:i64 -- r:i64) { 2 % 0 == } hof::bi
    and print nl
}
