use hof

fn main() {
    // dip applies function to second element, preserves top
    10 20 fn (x:i64 -- r:i64) { 2 * } hof::dip
    print " " print print nl

    // Process bottom while holding top
    3 7 fn (x:i64 -- r:i64) { dup * } hof::dip
    print " " print print nl
}
