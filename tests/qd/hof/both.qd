use hof

fn main() {
    // both applies same function to two values
    3 4 fn (x:i64 -- r:i64) { dup * } hof::both
    print " " print print nl

    // Double both values
    5 10 fn (x:i64 -- r:i64) { 2 * } hof::both
    print " " print print nl
}
