// Test: anonymous function calling a named function
stack fn helper(i64 -- i64) {
    10 +
}

fn main() {
    // Anonymous function that calls named function
    stack fn (i64 -- i64) { helper 2 * } -> process

    5 process call print nl
}
