// Test control flow inside anonymous functions
fn main() {
    // Anonymous function with conditional (uses stack, not named params)
    fn (i64 -- i64) {
        dup 0 > if {
            2 *
        } else {
            neg
        }
    } -> abs_double

    5 abs_double call print nl
    -3 abs_double call print nl
}
