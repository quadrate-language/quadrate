// Test: inline anonymous function (no variable)
fn main() {
    // Direct inline call
    5 stack fn (i64 -- i64) { 2 * } call print nl

    // More complex inline
    3 4 stack fn (i64 i64 -- i64) { * } call print nl
}
