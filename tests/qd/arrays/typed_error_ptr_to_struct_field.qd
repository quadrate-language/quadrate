// Test: old ptr variable to []i64 struct field should fail
use mem

struct Container {
	data:[]i64
}

fn old_make( -- arr:ptr) {
	24 mem::alloc!
}

fn main() {
	old_make -> x
	Container {
		data = x
	} -> c
	c <<data len print nl
}
