// Test: handle arguments with spaces
use os

fn main() {
	os::args -> args

	args len 0 > if {
		args 0 nth -> arg
		"Got: [" print arg print "]" print nl
	} else {
		"No arguments" print nl
	}
	args free
}
