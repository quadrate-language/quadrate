// Test: greet a person by name
use os

fn main() {
	os::args -> args

	args len 0 == if {
		"Usage: greet <name>" print nl
	} else {
		args 0 nth -> name
		"Hello, " print name print "!" print nl
	}
	args free
}
