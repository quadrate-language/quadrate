option('build_examples', type: 'boolean', value: false, description: 'Build examples')
option('build_tests', type: 'boolean', value: false, description: 'Build tests')
option('build_fuzz', type: 'boolean', value: false, description: 'Build fuzzing targets (requires clang)')
option('build_stdlib', type: 'boolean', value: true, description: 'Build the standard library modules. Disable to build only the core libraries, e.g. for an embedded target that embeds lib/interp and needs neither the modules nor their dependencies such as OpenSSL.')
option('stdlib_modules', type: 'array', choices: ['fmt', 'io', 'math', 'mem', 'net', 'os', 'signal', 'unicode', 'strings', 'strconv', 'testing', 'time', 'thread', 'tty', 'tls', 'http', 'log'], value: ['fmt', 'io', 'math', 'mem', 'net', 'os', 'signal', 'unicode', 'strings', 'strconv', 'testing', 'time', 'thread', 'tty', 'tls', 'http', 'log'], description: 'Which standard library modules to build when build_stdlib is on. An embedder lists the ones it links and needs none of the dependencies of the rest, such as OpenSSL for tls and http.')
option('build_compiler', type: 'boolean', value: true, description: 'Build the compiler back end (lib/llvmgen and lib/qd, which need LLVM). Disable to build only the runtime and interpreter. Implied by build_tools.')
option('build_tools', type: 'boolean', value: true, description: 'Build the command-line tools (implies build_compiler). Disable to build only the libraries, e.g. when cross-compiling for an embedded target.')
