# quadfmt

Quadrate code formatter.

## Usage

```bash
quadfmt [options] <file.qd>
```

## Options

- `-w`, `--write` - Format in place (default: print to stdout)

## Examples

```bash
quadfmt file.qd        # Preview formatted output
quadfmt -w file.qd     # Format file in place
```
