# Build outputs
build
dist
.intermediate
.vscode

# IDE/Editor
.cache/clangd/index
compile_commands.json

# Temporary files
.tmp*
.out/
qd_*/

# Shared libraries (built artifacts)
*.so

# AI assistant context (local only)
CLAUDE.md

# Standalone binary artifacts
main

# Meson subprojects (downloaded dependencies)
subprojects/*/
!subprojects/*.wrap
!subprojects/packagefiles/
subprojects/.wraplock

# Benchmark artifacts
benchmarks/arithmetic_qd_typeaware
benchmarks/arithmetic_v8_specialized

# Command binaries (built in-place)
cmd/quadmcp/quadmcp

# Fuzzer artifacts
crash-*
vgcore.*
oom-*
slow-unit-*
fuzz/
tests/fuzz/corpus/

# Floppy dist artifacts
distfiles/floppy/staging/
distfiles/floppy/*.iso
