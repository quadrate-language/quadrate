__c {
	#include <stdio.h>
	#include <stdarg.h>
}

fn print() {
	__c {
		va_list args;
		va_start(args, n);
		int columns = (int)va_arg(args, __qd_real_t);
		va_end(args);
		for (int i = 0; i < __qd_stack_ptr; ++i) {
			if (i % columns == 0) {
				printf("\n");
			} else {
				printf(" ");
			}
			printf("%f", __qd_stack[i]);
		}
		if (__qd_stack_ptr > 0) {
			printf("\n");
		}
	}
}
