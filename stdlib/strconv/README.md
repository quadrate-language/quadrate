# strconv

String conversions (`strconv::` module).

## Overview

Convert between strings and numeric types.

## Key functions

- `itoa`, `atoi` - Integer ↔ string (base 10)
- `format_int`, `parse_int` - Integer with custom base
