use http

fn handle_static(c:ptr -- ) {
	"/" "./" http::static
}

fn main() {
	"Quadrate Store" print nl
	"http://localhost:8081/" print nl

	http::engine -> e
	defer {
		e http::free_engine
	}

	e "/*" &handle_static http::GET

	e ":8081" http::run!
}
