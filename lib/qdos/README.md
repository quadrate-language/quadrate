# qdos

Operating system interface (`os::` module).

## Overview

System-level operations including process control, environment, and filesystem.

## Key Functions

- `exit` - Terminate program
- `system` - Execute shell command
- `getenv` - Environment variables
- `exists`, `delete`, `rename`, `copy` - File operations
- `mkdir`, `list` - Directory operations
