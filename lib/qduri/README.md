# qduri

URI parsing and building (`uri::` module).

## Overview

Parse, build, encode, and decode URIs.

## Key Functions

- `parse` - Parse URI string
- `build` - Construct URI from components
- `encode`, `decode` - URL encoding
- `get_scheme`, `get_host`, `get_path`, `get_query` - Component access
