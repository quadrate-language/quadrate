# qdrand

Random number generation (`rand::` module).

## Overview

Pseudorandom number generation with optional seeding.

## Key Functions

- `new` - Create new generator
- `with_seed` - Create seeded generator
- `int` - Random integer
- `range` - Random in range
- `bool` - Random boolean
