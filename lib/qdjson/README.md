# qdjson

JSON parsing (`json::` module).

## Overview

JSON document parsing and value extraction.

## Key Functions

- `parse` - Parse JSON string
- `get`, `get_str`, `get_int`, `get_float` - Value access
- `get_array`, `array_len`, `array_get` - Array access
- `free` - Release parsed document
