# qduuid

UUID generation (`uuid::` module).

## Overview

Generate and validate UUIDs.

## Key Functions

- `v4` - Generate random UUID
- `v4_seeded` - Generate with seed
- `is_valid` - Validate UUID string
