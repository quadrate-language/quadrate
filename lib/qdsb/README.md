# qdsb

String builder (`sb::` module).

## Overview

Efficient string construction for building strings incrementally.

## Key functions

- `new` - Create builder
- `append`, `append_char` - Add content
- `build` - Produce final string
- `clear` - Reset builder
- `len` - Current length
