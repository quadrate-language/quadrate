# qdflag

Command-line argument parsing (`flag::` module).

## Overview

Parse command-line arguments and flags.

## Key functions

- `parse` - Parse arguments
- `string`, `int`, `bool` - Get flag values
- `boolean` - Check flag presence
- `destroy` - Free parser
