# qdsort

Array sorting (`sort::` module).

## Overview

Sorting and searching operations for arrays.

## Key Functions

- `ints`, `ints_desc` - Sort integer arrays
- `min`, `max` - Find extremes
- `search` - Binary search
- `reverse` - Reverse array
