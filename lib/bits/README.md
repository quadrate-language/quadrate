# bits

Bitwise operations (`bits::` module).

## Overview

Bitwise logical and shift operations on integers.

## Key functions

- `and`, `or`, `xor`, `not` - Logical operations
- `lshift`, `rshift` - Shift operations
